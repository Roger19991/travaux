{% extends 'base.html.twig' %}

{% block title %}Créer un essieu{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Create Axles</h1>

   {{ form_start(form) }}
<div class="form-group">
    {{ form_label(form.designation) }}
    {{ form_widget(form.designation, {'attr': {'class': 'form-control', 'style': 'background-color: green;'}}) }}
</div>
<div class="form-group">
    {{ form_label(form.description) }}
    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'style': 'background-color: green;'}}) }}
</div>
<div class="form-group">
    {{ form_label(form.caracteristique) }}
    {{ form_widget(form.caracteristique, {'attr': {'class': 'form-control', 'style': 'background-color: green;'}}) }}
</div>
<button type="submit">Créer</button>
{{ form_end(form) }}

    {% if app.request.method == 'POST' %}

        
    {% endif %}
</div>
{% endblock %}
